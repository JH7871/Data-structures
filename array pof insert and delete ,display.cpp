#include<stdio.h>
int main()
{
int a[100];
int element ,i,loc,n;
printf("enter the size of the array");
scanf("%d";&n);
printf("enter the array of elements");
for (i=0;i<n;i++)
{
	scanf("%d";&a[i]);
	}
printf("enter the position to insect elements");
scanf("%d",&loc);
loc----,
for (i=n-1;i>=n;i--- )
{
printf("%d",a[i]);	
		}		
printf("enter elements to delete");
scanf("d%",&n);
for(i=0;i<n;i++)
{
if(a[i]==n1)
for (j=1;j<(n-1);j++)
{
	a[j]=a[j+1];
	}	
	break
	}			
}